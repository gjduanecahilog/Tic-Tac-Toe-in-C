---

## 📄 README_TICTACTOE.md: Console Tic-Tac-Toe Game

```markdown
# ❌⭕ C Console Tic-Tac-Toe Game

This is a two-player, console-based version of Tic-Tac-Toe implemented entirely in C.

## 🚀 Setup and Compilation

To compile and run this game, you need a C compiler (like **GCC**) installed on your system.

### Step 1: Save the Code

Save the C source code for the Tic-Tac-Toe game into a file named `tictactoe.c`.

### Step 2: Compile the Program

Open your terminal and use the following command to compile the source code:

```bash
gcc tictactoe.c -o tictactoe
